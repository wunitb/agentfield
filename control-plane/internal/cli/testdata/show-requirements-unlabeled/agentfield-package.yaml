config_version: v1
name: unlabeled-demo
version: 0.0.1
description: A node whose require_one_of group has no description.
main: app.py
user_environment:
  require_one_of:
    - id: llm_provider
      options:
        - name: ANTHROPIC_API_KEY
          description: Anthropic API key.
        - name: OPENROUTER_API_KEY
          description: OpenRouter API key.
